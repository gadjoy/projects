{
  "name": "client",
  "version": "1.0.0",
  "scripts": {
    "serve": "live-server --port=8080 --watch=public --open=public/index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
